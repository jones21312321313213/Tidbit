DELIMITER $$

CREATE PROCEDURE LoginUser(IN p_username VARCHAR(100))
BEGIN
    SELECT password
    FROM user_user
    WHERE username = p_username;

END$$
DELIMITER ;

just copy this in and make this in ur mySql if you need this
wat this do? authenticates the user whether username and password is correct