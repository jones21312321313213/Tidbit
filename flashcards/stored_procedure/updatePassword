DELIMITER $$

CREATE PROCEDURE update_userPassword(IN p_username VARCHAR(100))
BEGIN
    UPDATE user_user
    JOIN auth_user
        ON user_user.username = auth_user.username
    SET user_user.password = auth_user.password
    WHERE auth_user.username = p_username;
END $$

DELIMITER ;
